# Trading-Strategy-Backtest
A simple python code (streamlit app) to output expected balance against a historical set of data. Methods include SMA and EMA.

To run the programme, open on a new terminal and run:

$ streamlit run Homepage.py $
